# DESIGN.md — 百业百态 OFD 履约中心

## 项目定位
知味生态「百业百态 OFD 履约中心」——覆盖 O 需求单 / F 履约单 / J 工单三单闭环的履约协同平台，对接资源底座（ERP 管资源账、不吸业务流）。支持六大 DU（部门单元）归属、三类 HU（操作层身份）切换、角色权限矩阵与 48 小时自动验收。

## 气质与意象
一套「多角色共用的可发包履约工作台」，而非单机后台。以 Ant Design 5 为基础，主色为温暖的**活力橙 `#FF6B35`**，剥离科技蓝/紫，突出「每一单都有人负责、可点、可推进」的协作感。整体克制、内容密集、状态可读，弱化装饰。

## 视觉策略
- 以 AntD Layout 为骨架：左侧导航（按角色权限动态过滤菜单）、顶部用户区（HU 身份基地切换）。
- 列表主视图：数据密度优先，表格 + 状态 Tag + 操作为主的「工单台」风格；Detail 用 Descriptions/Steps 表达状态机。
- 状态一律用可读 Tag / 语义色，关键动作（推进、验收、转派）放行内操作，避免悬浮装饰。

## 配色方案
- 品牌主色：活力橙 `#FF6B35`（按钮/高亮/当前身份）。
- 语义状态色（AntD 语义，低饱和）：待办/默认中性、进行中 `#1677ff`、已完成 `#52c41a`、异常/红灯 `#ff4d4f`、预警（黄灯）`#faad14`。
- 红灯（履约风险预警）：`#ff4d4f` / 黄灯 `#faad14` / 待办白 `#ffffff`（三灯用于 Top 指标与 WorkOrder 卡片）。
- 背景白 / 内容卡白，线条 `#f0f0f0`，正文 `rgba(0,0,0,0.88)`。不做大面积深色渐变频。

## 字体排版
- 中文字体族：系统默认（AntD 默认 `-apple-system` 体系），数字统一 `tabular-nums` 对齐。
- 排版节奏：页面标题 20px 半粗；表格正文 13px；状态 Tag 小号等宽数字展示单号/时长；大指标卡用 24px+ 强调「当前在途/风险单量」。

## 动效与交互
- 状态流转：Steps/Tag 状态变化用 AntD 默认过渡，克制位移。
- 列表行悬停：AntD 表格默认高亮，不做放大投影。
- 弹窗/抽屉：用 AntD Modal/Drawer 承载新建与详情，滚动顺畅。
- 过渡统一克制的淡入/位移，禁止弹跳、跑马灯。

## 设计禁忌
- 禁止大面积科技蓝/紫渐变（非 AI 产品）。
- 禁止大圆角奶油卡片堆叠（非营销落地页）；卡片以 AntD Card 轻描边为准。
- 禁止把装饰当信息（无意义的图标浮层）；信息以 Tag/数字/状态灯表达。
- 禁止隐藏核心操作为只读态——当前身份有权限的动作必须可点、可推进。