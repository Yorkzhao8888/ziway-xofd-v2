# DESIGN.md — 知味生态 X-OFD v2 履约中枢

## 产品气质与意象

- **定位**：生鲜履约调度中枢，供生态内部履约/调度人员高频使用。不是对外营销站，而是"每天打开十几次的工作台"。
- **意象锚点**：清晨冷链分拣仓的调度台——灯光明亮、动线清晰、信息密度高但秩序井然；冷白色作业台面 + 绿色周转筐 + 黄色警示标签。像机场塔台/物流调度屏，而不是消费电商首页。
- **关键词**：清晰、高密度、状态可辨、少装饰、重效率。

## 视觉策略

- 以 Ant Design 5 中后台组件为底，深色侧边栏 + 亮色内容区，不做营销化大 Banner。
- O/F/J 三单各自有稳定的**色标**（用于菜单、徽标、状态点、表格行标识），三单并列时一眼区分；色标只用于标识与状态，不铺满页面。
- 数据展示优先：统计卡片 → 筛选条 → 高密度表格 → 抽屉详情，是每个单据列表页的统一骨架。

## Design Tokens

### 色彩（AntD ConfigProvider token）

- 品牌主色 `colorPrimary`：`#159947`（生态绿，意象：绿色周转筐 / 蔬菜溯源标），用于主按钮、选中态、链接。
- 深色侧边栏底：`#1f3d2b`（墨绿，调度台沉稳感），非纯黑。
- 内容区底色：`#f5f6f4`（带一丝绿灰的冷白，作业台面）。
- 三单色标（待发单方确认最终语义，骨架先占位）：
  - O 单：`#159947`（绿，订单源头）
  - F 单：`#2f6fed`（蓝，履约执行）
  - J 单：`#d48806`（琥珀黄，结算/作业单据，警示标签色）
- 状态语义色沿用 AntD 默认：success `#52c41a`、warning `#faad14`、error `#ff4d4f`、processing 蓝。
- 文字：正文 `rgba(0,0,0,0.88)`，次要 `rgba(0,0,0,0.45)`，禁止纯黑大字。

### 字体

- 字体族：`-apple-system, "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif`；数字单据编号/金额用 AntD 默认等宽数字呈现（`font-variant-numeric: tabular-nums`）。
- 不引入外部字体 CDN，保证内网/沙箱环境稳定。

### 间距与圆角

- 间距节奏遵循 AntD 8px 栅格；内容卡片圆角 8px，控件圆角 6px，不追求大圆角。
- 表格密度偏中高（`size="middle"`，关键列表可 `size="small"`），行距紧凑，一屏多看单据。

## 布局与响应式

- 经典中后台三段式：左侧固定深色 Sider（可折叠，200px/80px）+ 顶部 Header（56px，环境标识/用户信息）+ 内容区（内边距 16~24px）。
- 菜单分组：「概览」与「单据中心（O 单 / F 单 / J 单）」，三单为平级一级入口，突出闭环关系。
- 以桌面宽屏（1440px+）为主场景，保证 1280px 不错乱；移动端仅要求可查看，不做移动优先。

## 交互与状态

- 列表页统一交互：顶部统计/筛选条 → Table（分页、行选择、色标状态点）→ 行操作「详情」开 Drawer，不在骨架期跳复杂详情页。
- 全部异步操作有 loading 态、失败有 message/result 反馈；按钮防重复提交。
- 动效克制：仅用 AntD 默认过渡，不额外加视差/入场动画；刷新类操作不打断筛选状态（状态入 URL query 或 store）。

## 认证态

- 未认证时展示全屏「需要登录」Result 页（生态绿主按钮「前往统一认证登录」），不自建注册/登录表单。
- 认证信息来自 OAS（生态统一认证），前端不存储密码；登录跳转地址由后端配置下发/环境变量提供，骨架期按钮跳转地址可空但不得伪造登录成功。

## 设计禁忌

- 不要科技蓝紫渐变、不要玻璃拟态、不要大圆角漂浮卡片堆叠的营销风。
- 不要暗色主题为主（暗色仅限侧边栏/顶栏，内容区永远亮色，保证单据可读）。
- 不要用 emoji 代替状态标识，状态一律用色点 + Tag 文字。
- 不要在未拿到需求前臆造单据字段与业务流程图。
